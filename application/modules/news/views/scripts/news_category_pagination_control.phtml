<?php if ($this->pageCount > 1): ?>
    <!-- Previous page link -->
                        <?php if (isset($this->previous)): ?>
                                  <a class="prev page-numbers" href="<?php echo $this->url(array('slug' => $this->category,'page' => $this->previous), 'domain-news-category'); ?>"><i class="fa fa-caret-left"></i>Poprzednia</a>
                          <?php endif;?> 
                          <?php foreach ($this->pagesInRange as $page): ?>
                          <?php if($page!= $this->current): ?>
                                <a class="page-numbers" href="<?php echo $this->url(array('slug' => $this->category,'page' => $page), 'domain-news-category'); ?>"><?php echo $page; ?></a>
                          <?php else: ?>
                             <span class="page-numbers current"><?php echo $page; ?></span>
                          <?php endif; ?>
                          <?php endforeach; ?>
                          
                          <?php if (isset($this->next)): ?>
                                  <a class="next page-numbers" href="<?php echo $this->url(array('slug' => $this->category,'page' => $this->next), 'domain-news-category'); ?>">Następna<i class="fa fa-caret-right"></i></a>
                          <?php endif;?> 
                      
                
    <!-- Next page link -->
    
<?php endif; ?>
